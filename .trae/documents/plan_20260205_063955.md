He analizado la situación actual. Ya existe un proyecto en Neon llamado `transactions-dashboard` con la estructura de base de datos **ya creada** y alineada casi perfectamente con tus necesidades (tablas `transactions`, `banks`, `gmail_accounts`).

Esto acelera mucho el proceso. El plan se centra en conectar tu Frontend existente con esta Base de Datos y preparar el terreno para n8n.

### 1. Configuración de Base de Datos (Neon DB)
Usaremos el proyecto existente `transactions-dashboard`.
*   **Seed Data**: La tabla `banks` ya tiene "Wells Fargo" y "Bank of America". La tabla `gmail_accounts` está vacía. Insertaremos las 3 cuentas de correo (`personal`, `business`, `work`) para que coincidan con tus mocks.
*   **Conexión**: Generaremos la cadena de conexión y la configuraremos en tu entorno local (`.env.local`).

### 2. Integración Backend (Next.js + Neon)
Eliminaremos los datos falsos (`mock-data.ts`) y conectaremos la app a la base de datos real.
*   **Driver**: Instalaremos `@neondatabase/serverless` para una conexión eficiente y serverless-friendly.
*   **Data Fetching**: Crearemos una Server Action en Next.js que ejecute una consulta SQL con `JOINs` para traer la transacción junto con el nombre del banco y el email asociado.
*   **Actualización de UI**: Modificaremos el componente `Dashboard` para recibir estos datos reales de forma asíncrona.

### 3. Testing (Vitest)
Cumpliendo con tu requisito de auditar cada paso.
*   **Instalación**: Configuraremos Vitest y React Testing Library en el proyecto Next.js.
*   **Unit Tests**: Crearemos un test para verificar que el Dashboard renderiza correctamente la información de las transacciones (usando mocks para la llamada a base de datos en el test).

### 4. Preparación para n8n
Para que tu automatización en n8n funcione, necesitará insertar datos respetando las relaciones (Foreign Keys).
*   Proveeré la **Query SQL exacta** que n8n debe ejecutar para guardar una transacción. Esta query buscará automáticamente el `id` del banco y del email basándose en el texto (ej. "Wells Fargo") para mantener la integridad de la base de datos.

### Pasos de Ejecución
1.  **DB Seed**: Insertar cuentas de gmail y algunas transacciones de prueba.
2.  **Dependencies**: Instalar driver de DB y librerías de testing.
3.  **Code**: Implementar `lib/db.ts` y lógica de fetching.
4.  **Refactor**: Conectar UI a datos reales.
5.  **Test**: Ejecutar auditoría con Vitest.
